<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.mindquest</groupId>
    <artifactId>mindquest-frontend</artifactId>
    <version>0.1.0</version>
    <packaging>pom</packaging>

    <name>MindQuest Frontend (placeholder)</name>
    <description>Placeholder Maven module for the frontend assets. The real frontend build uses Bun/Vite.</description>

    <!--
      This is a minimal placeholder POM so the top-level aggregator can include
      the `frontend` directory as a module without Maven failing when no Java
      sources exist. If you want Maven to drive the frontend build, we can add
      executions here to run Bun/npm/yarn (conditional on an active flag).
    -->

</project>
